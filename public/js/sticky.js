const body = document.querySelector("body");

function createSticky() {
  const writePad = createBox();
  const textarea = document.createElement("textarea");
  writePad.appendChild(textarea);
}